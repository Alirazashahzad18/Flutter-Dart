import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter App',
      theme: ThemeData(colorScheme: .fromSeed(seedColor: Colors.deepPurple)),
      home: const MyHomePage(title: 'Flutter Demo Home Page'),
    );
  }
}

class MyHomePage extends StatefulWidget {
  const MyHomePage({super.key, required this.title});

  final String title;

  @override
  State<MyHomePage> createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Theme.of(context).colorScheme.inversePrimary,

        title: Text(widget.title),
      ),
      // ignore: avoid_unnecessary_containers
      body: Container(
        child: Column(
          children: [
            ElevatedButton(onPressed: () {}, child: Text("bot")),
            SizedBox(height: 10),
            Text("button", style: TextStyle(fontSize: 25, color: Colors.green)),
            SizedBox(height: 10),
            Text("Padding"),
            SizedBox(height: 10),
            Text("push", style: TextStyle(fontSize: 10, color: Colors.red)),
            SizedBox(height: 15),
            Text("Delete", selectionColor: Color.fromARGB(0, 5, 5, 232)),
            SizedBox(height: 20),
            Text("Enter", style: TextStyle(fontSize: 20)),
            TextButton(onPressed: () {}, child: Text("Extract")),
            SizedBox(height: 20),
            Text("extract", style: TextStyle(fontSize: 30, color: Colors.blue)),
          ],
        ),
      ),
    );
  }
}
